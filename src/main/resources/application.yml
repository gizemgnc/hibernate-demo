spring:
  datasource:
    url: jdbc:mysql://localhost:3306/CUSTOMER
    password: 419Gizem
    username: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
server:
  port: 8081